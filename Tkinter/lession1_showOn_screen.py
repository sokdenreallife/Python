from tkinter import * # * Symbol meant import everything or often packages

root = Tk()

# Declare variables (Create a label widget)
myLabel = Label(root, text = "Hello World!")

# Showing it onto screen
myLabel.pack()

root.mainloop()